interface:
  display_name: "SQL Server to DBX Converter"
  short_description: "Convert SQL Server SQL into Databricks SQL"
  default_prompt: "Use $sqlserver-to-dbx-converter to convert this SQL Server query into Databricks SQL."
